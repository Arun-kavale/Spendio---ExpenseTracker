/**
 * Central export for app assets.
 * Use these in components for consistent branding.
 */

// Spendio logo (icon + wordmark) – use for headers, about, onboarding
// Use filename without spaces so React Native loads it reliably on all devices
// eslint-disable-next-line @typescript-eslint/no-var-requires
export const SpendioLogo = require('./logo/SpendioLogo.png');
